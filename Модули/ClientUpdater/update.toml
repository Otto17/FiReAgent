# Copyright (c) 2025-2026 Otto
# Лицензия: MIT (см. LICENSE)


# Версия релиза (формат "дд.мм.гг")
version = "01.02.26"

# -----------------------------
# 📁 Секция файлов [[files]]
# -----------------------------

# Самообновление утилиты ClientUpdater
[[files]]
Src    = "ClientUpdater.exe"     # В архиве: FiReAgent/ClientUpdater.exe
Dest   = "ClientUpdater.exe"     # На диске: C:\Program Files\FiReAgent\ClientUpdater.exe
Action = "update"

# Обновление FiReAgent
[[files]]
Src    = "FiReAgent.exe"
Dest   = "FiReAgent.exe"
Action = "update"

# Обновление модуля ModuleCrypto.exe
[[files]]
Src    = "ModuleCrypto.exe"
Dest   = "ModuleCrypto.exe"
Action = "update"

# Удаление устаревшего файла конфигурации
[[files]]
Dest   = "config/тестовый файл.txt"	# На диске
Action = "delete"

[[files]]
Dest   = "ss.NONE"	# На диске
Action = "delete"

# Обновление конкретного конфига
[[files]]
Src    = "config/Профиль Тест24.rpf"
Dest   = "config/Профиль Тест24.rpf"
Action = "update"



# =================================================================
# 📚 ДОКУМЕНТАЦИЯ И ПРИМЕРЫ
# =================================================================


# ПРИМЕЧАНИЯ:
# • Архив должен содержать: [update.toml] (в корне) и папку [FiReAgent/ ...файлы...].
#   В полях Src указывается путь ОТНОСИТЕЛЬНО папки "FiReAgent/" (без префикса).
# • Все операции выполняются строго внутри "C:\Program Files\FiReAgent".
#   Любые пути Dest вне этой директории будут отклонены.
# • Для Action="update" можно не указывать Dest — тогда он берётся как путь из Src.
# • Для Action="delete" Src не используется — обязателен Dest.
# • Операции выполняются по порядку сверху вниз.
# • В TOML проще и безопаснее использовать правые "прямые" слеши '/' ("plugins/driver.dll"), несмотря на то, что работа происходит в Windows.
#   Обратные слеши '\' требуют экранирования: "plugins\\driver.dll".


# -----------------------------
# 📁 СЕКЦИЯ ФАЙЛОВ [[files]]
# -----------------------------

# КЛЮЧИ:
#  Src           	– путь в архиве (относительно корневой папки "FiReAgent/" в архиве) — используется только для update
#  Dest          	– путь назначения внутри "C:\Program Files\FiReAgent". Можно указать абсолютный путь, НО только внутри базовой директории FiReAgent!
#  Action=update	– заменить/создать файл (обновление)
#  Action=delete	– удалить файл/папку

# ДОП. КЛЮЧИ ДЛЯ ОБНОВЛЕНИЯ ПАПКИ:
#  IsDir   = true   - это папка? (true - да / false - нет, это файл)
#  Replace = true   – удалить старое содержимое папки перед копированием
#  Replace = false  – только добавить/перезаписать файлы (не удаляя существующие файлы)



# ➡️ ПРИМЕРЫ:
# Удаление файла (относительный путь внутри базовой директории):
# [[files]]
# Dest   = "config/old_debug.json"
# Action = "delete"

# Удаление целой директории "legacy" (рекурсивно):
# [[files]]
# Dest   = "plugins/legacy"
# Action = "delete"

# Обновление файла (короткая форма — без Dest, будет использован путь из Src):
# [[files]]
# Src    = "tools/diag.exe"
# Action = "update"

# Обновление файла по абсолютному пути (допускается только внутри "C:\Program Files\FiReAgent"):
# [[files]]
# Src    = "plugins/driver.dll"
# Dest   = "C:/Program Files/FiReAgent/plugins/driver.dll"
# Action = "update"

# Обновление нескольких ресурсов:
# [[files]]
# Src    = "bin/agent_core.dll"
# Dest   = "bin/agent_core.dll"
# Action = "update"
#
# [[files]]
# Src    = "resources/ui/main.txt"
# Dest   = "resources/ui/main.txt"
# Action = "update"

# Удаление временного мусора:
# [[files]]
# Dest   = "temp/install.cache"
# Action = "delete"

# Полная замена папки (удалить старое + скопировать новое):
# [[files]]
# Src     = "data/test"
# Dest    = "data/test"
# Action  = "update"
# IsDir   = true 			# Это папка
# Replace = true			# Удалить старое содержимое перед копированием

# Обновление папки (НЕ удалять старые, добавить новые, перезаписать существующие):
# [[files]]
# Src     = "config/templates"
# Dest    = "config/templates"
# Action  = "update"
# IsDir   = true			# Это папка
# Replace = false			# Не удалять существующие файлы

# Обновление папки с абсолютным путём:
# [[files]]
# Src     = "plugins"
# Dest    = "C:/Program Files/FiReAgent/plugins"
# Action  = "update"
# IsDir   = true			# Это папка
# Replace = true			# Удалить старое содержимое перед копированием
